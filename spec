types:
    literal types:
        void
        boolean
        i8 i16 i32 i64
        u8 u16 u32 u64
        f32 f64
        string
    typeinfo types (compile time):
        builtin
        instance
    ast node types (compile time):
        literal
        symbol
        path
        call
        code

lookup modes:
    global, mixed, local
